{
  "name": "@activit/api",
  "type": "module",
  "main": "dist/**/*.js",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc && cpy \"src/**/*.json\" host.json package.json dist",
    "start": "func start",
    "watch": "tsc -w",
    "prestart": "npm run clean && npm run build",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.7.2"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "azure-functions-core-tools": "^4.1.x",
    "cpy-cli": "^5.0.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.8.0"
  }
}
